// Default social share card (rendered by app/opengraph-image.tsx).
// Pages that define their own `openGraph` block replace the inherited one,
// so they must include this explicitly to keep the card.
export const defaultOgImage = {
  url: '/opengraph-image',
  width: 1200,
  height: 630,
  alt: 'The Muslim Will — Will + Vault',
}
